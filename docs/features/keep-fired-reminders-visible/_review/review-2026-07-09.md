---
status: PASS
date: "2026-07-09"
reviewer: "sdd:reviewer (independent, clean-context) + Tech Lead resolution pass"
---

# Review — keep-fired-reminders-visible (2026-07-09, re-review)

## Scope

`git diff master...HEAD` on `feature/keep-fired-reminders-visible`, 12 commits, 22 files changed (+375/-203) — the full feature diff, including the two review-fix commits from the earlier same-day pass:

```
 docs/features/keep-fired-reminders-visible/sad.md  | 26 +++----
 .../keep-fired-reminders-visible/tasks.json        | 28 ++++++++
 .../keep-fired-reminders-visible/tasks/tracker.md  | 22 +++---
 docs/ops/backups.md                                | 53 +++++++-------
 src/app/ports/reminder-repository.ts               |  7 +-
 .../__tests__/active-pending-ordering.test.ts      | 65 -----------------
 .../use-cases/__tests__/helpers/in-memory-repo.ts  | 11 +--
 .../__tests__/list-active-reminders.test.ts        | 81 ++++++++++++++++++++
 .../use-cases/__tests__/resolve-reminder.test.ts   | 12 +++-
 src/app/use-cases/list-active-reminders.ts         | 16 +++--
 src/app/use-cases/resolve-reminder.ts              | 14 ++--
 src/infra/__tests__/grammy-gateway.test.ts         | 10 +--
 .../__tests__/sqlite-reminder-repository.test.ts   | 58 ++++++++--------
 src/infra/db/sqlite-reminder-repository.ts         | 10 +--
 src/infra/telegram/grammy-telegram-gateway.ts      |  7 +-
 src/ports/__tests__/list-handler.test.ts           | 49 ++++++++++++
 src/ports/__tests__/resolve-handler.test.ts        | 34 ++++++---
 src/ports/__tests__/router-auth.test.ts            | 11 +++
 src/ports/handlers/list-handler.ts                 | 23 ++++--
 src/ports/handlers/resolve-handler.ts              | 25 ++++--
 test/integration/e2e-capture-fire-resolve.test.ts  | 14 ++--
 test/integration/webhook-wake-perimeter.test.ts    |  2 +-
 22 files changed, 375 insertions(+), 203 deletions(-)
```

SDD-AC trailers claimed across all commits: AC-01..AC-08, plus AC-03/AC-04 (T10) and AC-06 (T11) on the review-fix tasks.

This is a **re-review** after `implement` closed both Fix-now findings from the earlier same-day pass (`review-2026-07-09.md` v1, superseded by this file): Finding 1 (`firing`-state reminders excluded from the list) fixed by commit `a5ab737` (T10), Finding 2 (stale `done` tap on a forged `reminderId` crashing) fixed by commit `3e70ef5` (T11).

## AC / user-story trace

All 8 acceptance criteria and all 5 user stories (US-01..US-05) trace end-to-end to production code, a behaviour-asserting test, and a `sad.md` §6 flow. Verified independently (clean-context dispatch, re-derived from `spec.md`/`sad.md`/`data-model.md`/`CONTEXT.md`/`tasks.json`, not from the prior review's summary) against the current code.

- AC-01 — satisfied. `list-active-reminders.ts:72` (`findVisibleOrdered`); real-SQLite coverage in `sqlite-reminder-repository.test.ts`.
- AC-02 — satisfied. `list-active-reminders.ts:44` status field (`pending`→`scheduled`, else→`fired`); `list-handler.ts` renders 🔔/🕒.
- AC-03 — **now satisfied including the `firing` window.** Position stability asserted across `startFiring()` transitions at `list-active-reminders.test.ts:151`; query scope independently covered against real SQLite.
- AC-04 — **now satisfied.** `findVisibleOrdered` reads `state IN ('pending','firing','fired')` (`sqlite-reminder-repository.ts:117`); a reminder stuck retrying delivery (ADR-0005) stays visible. `sad.md` §5/§6 reconciled to match.
- AC-05 — satisfied. `list-handler.ts` omits Cancel on fired rows, keeps Source.
- AC-06 — **now satisfied for the forged/non-existent-id edge case too.** `resolve-reminder.ts:14` throws `ReminderNotFoundError` (not a generic `Error`) on a missing id; `resolve-handler.ts:26-33` catches `action === "done" && (InvalidStateTransitionError || ReminderNotFoundError)` → uniform toast, never reaches `bot.catch`. Directly asserted at `resolve-handler.test.ts:79`.
- AC-07 — satisfied. `router-auth.test.ts:174` extended with a fired-reminder fixture.
- AC-08 — satisfied, row-cap and char-budget truncation both exercised with real assertions.
- ADR-0001 (Done retirement), ADR-0002 (capture-order id ASC) — correctly implemented; `findActivePendingOrdered` fully superseded, no dead references.

## Findings

### Finding 1 (this pass) — `data-model.md` describes the pre-fix query scope (Stage 2, non-blocking)

**Files:** `docs/features/keep-fired-reminders-visible/data-model.md:55,63,83`

Three occurrences still stated the widened read as `state IN ('pending','fired')`, omitting `firing` — left behind when commit `a5ab737` reconciled `sad.md` but missed `data-model.md`. Contradicted the shipped code (`sqlite-reminder-repository.ts:117`), `sad.md` §12, and `CONTEXT.md`, all of which already correctly include `firing`.

**Verdict:** CONFIRMED. **Resolution: Fix now.** Updated all three occurrences to `IN ('pending','firing','fired')`.

### Finding 2 (this pass) — repository port doc comment omits `firing` (Stage 2, non-blocking)

**Files:** `src/app/ports/reminder-repository.ts:22`

`findVisibleOrdered()`'s doc comment read "All Visible (`pending` or `fired`, not yet explicitly deleted)", missing the `firing` state the method actually returns per `CONTEXT.md`'s canonical "Visible reminder" definition.

**Verdict:** CONFIRMED. **Resolution: Fix now.** Comment updated to "`pending`, `firing`, or `fired`".

## Stage 2 — quality (no other findings)

Hexagonal layering respected. Owner-only gate unaffected (re-pinned with a fired-reminder fixture). No weakened test assertions — the e2e journey was strengthened (guarded-done rejected, then delete resolves) rather than loosened. Dormant `done` domain state has no production call site, deliberately retained per ADR-0001/spec §8. One non-blocking observation (not raised as a finding): AC-04's full fire→deliver→snooze→delete scenario is exercised at the app layer via the in-memory repo rather than real-SQLite integration; the query scope itself is independently covered against real SQLite, so behaviour is genuinely asserted — a level split, not a gap.

## Gate result

**PASS** — both findings from this re-review were doc-only, both resolved Fix-now inline (no re-entry into implement's TDD loop needed — no test or production-code change). Both Fix-now findings from the prior same-day review (`firing`-state visibility, forged-id crash) are independently confirmed resolved. No open Stage-1 (AC) finding remains.
