# Data-model audit — keep-fired-reminders-visible — 2026-07-09

## Summary

Thin reconciliation pass, as predicted by `sad.md` §4 ("No schema change and no new migration are implied by this feature"). **No migrations are staged** — `docs/features/keep-fired-reminders-visible/migrations/` was not created since there is nothing to stage. `implement`'s `layer: migration` task for this feature (if one is even generated by `tasks`) has nothing to promote.

## Convention source

- `docs/architecture-map.md` §Migrations + live `migrations/` (`01_create_owner_settings` … `04_create_pending_prompt`, Flyway-style `NN_<verb>_<entity>.{up,down}.sql`) — corroborated, not diverged from (moot here, no new file).
- `sad.md` §4/§5/§8 persistence decisions — confirms `id` (existing `INTEGER PRIMARY KEY AUTOINCREMENT`) already satisfies ADR-0002's capture-order key; no PK/audit/delete-strategy change requested.
- No Accepted ADR in this feature's `adr/` calls for a schema change (ADR-0001 is a `ports`-level guard, ADR-0002 confirms the existing `id` column suffices).

## Entities reviewed

- `reminders` — no column/constraint change. Query scope widens (`state = 'pending'` → `state IN ('pending','fired')`), which is application code (repository query), not DDL.
- `source_snapshots` — untouched.

## Drift detection

Domain `Reminder` type (`src/domain/reminder.ts`) + `state-machine.ts` states vs. `reminders` table columns: **no drift** (`id`, `snapshot_id`, `state`, `scheduled_at`, `fired_at`, `delivered_at`, `fired_message_id`, `created_at` all map 1:1, nullability matches). No `_drift/` fix migrations generated.

## Index decision

Considered `idx_reminders_state` (or a composite) to back `WHERE state IN ('pending','fired') ORDER BY id ASC`. **Rejected**: `id` is the SQLite rowid alias (`INTEGER PRIMARY KEY`), so physical row order already satisfies `ORDER BY id ASC` with no index; the remaining full-table-scan filter cost is negligible at this bot's single-Owner scale and within the unchanged p95 ≤ 1000 ms NFR. Documented in `data-model.md` § Indexes as a considered-and-rejected candidate, with a revisit trigger (row-count growth) rather than a silent omission.

## Self-checks (stack-agnostic, §12 of the skill)

| Check | Result |
|---|---|
| Naming matches repo convention | N/A — no new file written |
| Down reversibility (every CREATE has a DROP, etc.) | N/A — no new migration |
| FK indexes (every `REFERENCES` has a backing index) | Already satisfied by existing `idx_reminders_snapshot_id`; unchanged |
| Convention adherence (repo's detected conventions followed, no house style imposed) | N/A — no schema authored |

All four pass trivially since no DDL was written; no divergence to flag.

## Promote-time hint

Not applicable — no staged migration file exists for `implement` to promote. If a future `tasks` pass adds a `layer: migration` task anyway, it should be marked no-op / skipped, since `data-model.md` has already confirmed no schema change is required.

## `<!-- TBD -->` markers

None — every entity/column decision in `data-model.md` is resolved, not deferred.

## Next stage

`api keep-fired-reminders-visible` — expected to also be a thin pass or a no-op, since this is an internal bot command surface with no OpenAPI-relevant public contract change; confirm during that stage rather than assuming here.
