# Review — telegram-reminder — 2026-06-13

## Scope

- Branch: `feat/telegram-reminder` vs `master`
- Commits: T01–T14 (15 commits)
- Diff stat: 70 files changed, 5729 insertions(+), 14 deletions(-)
- Reviewer: `sdd:reviewer` (clean-context, independent)

---

## Stage 1 — AC Completeness

| ID | Severity | Finding | AC | File:line | Verdict |
|----|----------|---------|-----|-----------|---------|
| S1-1 | 🔴 CRITICAL | `evening`/`tomorrow`/`custom` quick-pick callbacks dead-end in router `else` branch — reminder never scheduled | AC-02, AC-03, AC-08 | `src/ports/router.ts:65-72` | **Fix now** |
| S1-2 | 🔴 CRITICAL | `handleSnoozePick` sends new message but never edits original fired-reminder message — live buttons remain | AC-05 | `src/ports/handlers/snooze-handler.ts:70-80` | **Fix now** |
| S1-3 | 🟠 MAJOR | `snooze_pick:` callback prefix not routed — `handleSnoozePick` never invoked in production | AC-05 | `src/ports/router.ts:40-51` | **Fix now** |
| S1-4 | 🟠 MAJOR | `evening`/`tomorrow` wall-clock computed in server local time, not Owner IANA timezone | AC-02 | `src/ports/handlers/snooze-handler.ts:62-65` | **Fix now** |
| S1-5 | 🟠 MAJOR | AC-12 (protected-content note) implemented but no test asserts it | AC-12 | `src/infra/telegram/grammy-telegram-gateway.ts:24-27` | **Fix now** |
| S1-6 | 🟡 MINOR | "Hide past wall-clock" check uses server local time instead of Owner timezone | AC-05 | `src/ports/handlers/snooze-handler.ts:23` | **Fix now** (bundled with S1-4) |

---

## Stage 2 — Code Quality

| ID | Severity | Finding | File:line | Verdict |
|----|----------|---------|-----------|---------|
| S2-1 | 🟠 MAJOR | Non-Owner callbacks (snooze/done/delete/source/qpick) not auth-gated — `auth-middleware.ts` never wired into router | `src/ports/router.ts:24-54` | **Fix now** |
| S2-2 | 🟠 MAJOR | `main.ts` bootstraps timezone as `""` instead of `null` — contradicts data-model, fragile falsy logic | `src/main.ts:31-34` | **Fix now** |
| S2-3 | 🟠 MAJOR | `delivered_at`/durability tests are tautological — test seeds `state:'fired'` which is already filtered by `findDuePending`; guard is untested | `test/integration/durability.test.ts:94` | **Fix now** |
| S2-4 | 🟡 MINOR | `messageId !== null` — dead check, `messageId` typed `number` (non-nullable) | `src/domain/value-objects/source-snapshot.ts:16` | **Deferred** — spec §8, owner: Mykhailo Podaniev, due: before next release |
| S2-5 | 🟡 MINOR | Use cases throw raw `new Error(...)` instead of domain sentinels (schedule-reminder, snooze-reminder, resolve-reminder) | `src/app/use-cases/schedule-reminder.ts:14` | **Deferred** — spec §8, owner: Mykhailo Podaniev, due: before next release |
| S2-6 | 🟡 MINOR | `scheduleUC` dead parameter in `capture-conversation.ts:71-75` — scheduling unreachable via this path | `src/ports/conversations/capture-conversation.ts:71` | **Deferred** — spec §8, owner: Mykhailo Podaniev, due: before next release |

---

## Verdict

**CHANGES REQUESTED**

Domain, app use-cases, repository, and migrations are clean and well-tested in isolation. All 71 tests pass (`tsc` clean). However, the ports/wiring layer has critical end-to-end gaps: two quick-pick variants dead-end, custom-time flow missing entirely, snooze_pick unrouted, snooze never edits the fired message, callbacks not auth-gated. Unit tests pass by invoking handlers directly, masking the missing router branches.

**To resolve:** fix all 8 "Fix now" findings via `/sdd:implement telegram-reminder`, then re-review the changed surface.
