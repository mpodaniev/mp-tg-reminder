# Review — telegram-reminder — 2026-06-13 (re-review after T15/T16/T17)

## Scope

- Branch: `feat/telegram-reminder` vs `master` (base `bdb32a2`)
- Diff: 75 files changed, ~6625 insertions(+), 15 deletions(-)
- Reviewer: `sdd:reviewer` (clean-context, independent) — re-read spec/sad/data-model/contracts/ADRs itself
- Gates run: `npx tsc --noEmit` clean; `npx vitest run` → 24 files, **99 tests passing**
- This pass re-verifies the 8 "Fix now" findings from `review-2026-06-13.md` and traces the whole §4/§5 set end-to-end.

---

## Prior "Fix now" findings — verification (all resolved)

| Prior ID | Finding | Resolved? | Evidence |
|----------|---------|-----------|----------|
| S1-1 | evening/tomorrow/custom quick-picks dead-end in router | ✅ | `src/ports/router.ts:78` routes `qpick`; `handleQuickPick:85-131` |
| S1-2 | snooze never edits original fired-reminder message | ✅ | `src/ports/handlers/snooze-handler.ts:82-96`; test `snooze-handler.test.ts` |
| S1-3 | `snooze_pick:` prefix not routed | ✅ | `src/ports/router.ts:62-70` |
| S1-4 | evening/tomorrow in server local time, not Owner tz | ✅ | `localTodayAt(timezone,…)` `router.ts:109-112`, `snooze-handler.ts:68-71` |
| S1-5 | AC-12 protected-content note untested | ✅ | `grammy-gateway.test.ts` asserts restriction note + 4 buttons |
| S1-6 | hide-past-wall-clock in server local time | ✅ | `getOwnerHour(timezone,…)` `snooze-handler.ts:31` |
| S2-1 | non-Owner callbacks not auth-gated | ✅ | `router.ts:48-51`; test `router-auth.test.ts` |
| S2-2 | main.ts seeds timezone `""` not `null` | ✅ | `main.ts` `upsertOwnerSettings(…, null)` |
| S2-3 | tautological delivered_at/durability test | ✅ | `durability.test.ts` re-fires a genuine `state='firing'` row + real at-least-once edge |

---

## Stage 1 — end-to-end AC trace (whole §5 set, not just claimed)

| AC | Code | Test | Status |
|----|------|------|--------|
| AC-01 | `capture-conversation.ts:70-116` | `capture-conversation.test.ts` | OK |
| AC-02 | `router.ts:85-131` | `snooze-handler.test.ts`, e2e | OK |
| AC-03 | `router.ts:133-183` + `parseCustomTime` | `custom-time-conversation.test.ts` | OK |
| AC-04 | `fire-due-reminders.ts` | `fire-due-reminders.test.ts`, e2e | OK |
| AC-05 | `snooze-handler.ts` + `snooze-reminder.ts` | `snooze-handler.test.ts`, e2e | OK |
| AC-06 | `resolve-handler.ts` (delete + 48h edit) | `resolve-handler.test.ts` | OK |
| AC-07 | `resolve-handler.ts` | `resolve-handler.test.ts` | OK |
| AC-08 | `router.ts:156-160` + `ScheduledTime` | `custom-time-conversation.test.ts`, `schedule-reminder.test.ts` | OK |
| AC-09 | `capture-message.ts` + `router.ts:48-51` | `router-auth.test.ts`, `capture-message.test.ts` | OK |
| AC-10 | `snooze-handler.ts:23-26` + `router.ts` snooze-custom catch | `snooze-handler.test.ts`, `custom-time-conversation.test.ts` (snooze-via-custom) | OK |
| AC-11 | `source-handler.ts`, `source-snapshot.ts` | `source-handler.test.ts` | OK |
| AC-12 | `grammy-telegram-gateway.ts` | `grammy-gateway.test.ts` | OK |
| AC-13 | `capture-message.ts` + `capture-conversation.ts` | `capture-conversation.test.ts` | OK |

US-01..US-08 each reach ≥1 AC + a §6 flow. No story or AC dropped from the chain. `target_surfaces: [backend-service]` — no UI surface, so no component/e2e-through-UI tests required.

---

## Findings this pass + verdicts

| ID | Stage | Severity | Finding | File:line | Verdict |
|----|-------|----------|---------|-----------|---------|
| F1 | 2 | 🟡 MINOR | dead `scheduleUC` param (deferred S2-6 whose due-trigger had fired) | `src/ports/conversations/capture-conversation.ts:74` | **Fixed now** — param removed, call site + test updated; §8 S2-6 closed |
| F2 | 2 | 🟡 MINOR | snooze-via-custom-time path lacked `AlreadyResolvedError` catch → AC-10 message would be bypassed to `bot.catch` on a resolved-mid-flow reminder | `src/ports/router.ts:174-182` | **Fixed now** — wrapped in `AlreadyResolvedError` catch; added AC-10 test on this path |

No CRITICAL or MAJOR findings.

## Deferred items still tracked in spec §8

- **S2-4** — dead `messageId !== null` guard in `source-snapshot.ts` — still tracked, due before next release.
- **S2-5** — use cases throw raw `new Error(...)` on not-found (typed domain sentinels not yet introduced) — still tracked, due before next release. F2 added a port-level catch for the snooze-custom AC-10 slice, but the broader error-mapping cleanup remains open.

---

## Verdict

**PASS**

All 8 prior "Fix now" findings are genuinely resolved with tests that assert business-observable outcomes (not tautologies). All 13 ACs trace end-to-end to real code + an asserting test; all 8 user stories have an AC and a §6 flow. `tsc` clean, 99/99 tests green. The two MINOR findings raised this pass were fixed in-context. S2-4 and S2-5 remain explicitly deferred with owner + due in spec §8.

**Next:** `/clear`, then `/sdd:ship telegram-reminder`.
