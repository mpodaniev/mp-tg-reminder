# Review — list-active-reminders — 2026-06-15

**Reviewer:** independent clean-context (`sdd:reviewer`), not the author.
**Verdict:** **CHANGES REQUESTED**

## Scope

- Branch `feature/list-active-reminders`, base `master` (merge-base `ec9d326`).
- Diff: **20 files changed, 937 insertions(+), 11 deletions(-)**.
- `SDD-AC` trailers claim AC-01..AC-08 (all). Whole §4 US set + §5 AC set traced end-to-end.
- Baseline read: spec.md (§4/§5/§6/§6.1 + Test plan), sad.md (§6 five flows + cross-cutting, `target_surfaces: [backend-service]`), ADR-0001/0002/0003 (all Accepted), tasks.json (T1–T8).

## Stage 1 — US / AC end-to-end trace (the gate)

| ID | Trace (spec → flow → code → test) | Status |
|---|---|---|
| US-01 / AC-01 | Flow 1 → `findActivePendingOrdered` (`sqlite-reminder-repository.ts:107`, `ORDER BY scheduled_at ASC, r.id ASC`) → `ListActiveReminders` (`list-active-reminders.ts:65`) → `renderListMessage`/`formatFireTime` (`list-handler.ts:27,39`). Tests: ordering (`active-pending-ordering.test.ts`, real-SQLite `sqlite-reminder-repository.test.ts:169`), preview cap (`list-active-reminders.test.ts:68`), tz render (`list-handler.test.ts:58`). | PASS |
| US-04 / AC-02 | Flow 3 → `isEmpty` (`list-active-reminders.ts:69`) → `EMPTY_MESSAGE` (`list-handler.ts:76`). Test: `list-handler.test.ts:49`. | PASS |
| US-02 / AC-03 | Flow 2 happy → `Reminder.cancel()` (`reminder.ts:101`) → `CancelPendingReminder` (`cancel-pending-reminder.ts:18`) → confirm (`list-handler.ts:100`). Tests: `cancel-pending-reminder.test.ts:36`, `list-handler.test.ts:130`, `list-router.test.ts:83`. "List unchanged" enforced by construction, asserted only implicitly. | PASS |
| US-02 / AC-04 | Flow 2 else → domain guard (`state-machine.ts:30`) → re-throw sentinel → `NOT_ACTIVE_MESSAGE` (`list-handler.ts:104`). Tests: `state-machine.test.ts:67`, `reminder.test.ts:56`, `cancel-pending-reminder.test.ts:53`, `list-handler.test.ts:143,160`. Every end state exercised. | PASS |
| US-01,US-02 / AC-05 | `/list` gate `isOwner` (`list-handler.ts:73`); callback gate `router.ts:59`. Tests: `list-handler.test.ts:95`, `list-router.test.ts:76,92`, `router-auth.test.ts`. | PASS |
| US-03 / AC-06 | Flow 4 → router routes `source:<id>` to `handleSource` (`router.ts:87`) → `hasPublicDeepLink` else branch (`source-handler.ts:30`). Tests: `list-handler.test.ts:179,191` (both branches). | PASS |
| US-05 / AC-07 | Flow 5 → router `/list` (`router.ts:43`), menu registered `main.ts:57`. Test: `list-router.test.ts:65` (identical text both paths). | PASS (weak — menu path simulated as a second `/list` text msg; no test exercises actual `setMyCommands`) |
| US-01 / AC-08 | ADR-0003 → `truncateToFit` (`list-active-reminders.ts:46`, cap `MAX_ACTIVE_LIST_ROWS=50` + 4096 budget) → overflow line (`list-handler.ts:55`). Tests: row-cap + 4096-budget (`list-active-reminders.test.ts:78,92`), overflow + 1-message (`list-handler.test.ts:83`). | PASS |

**No Stage-1 gate failure** — all 5 user stories and all 8 ACs trace end-to-end with an asserting test; the `backend-service` surface incl. ports-layer rendering/keyboard behavior is covered by handler + router tests. `tsc --noEmit` clean; 148/148 tests pass.

## Stage 2 — findings & resolutions

| # | Sev | Finding (cited) | Resolution |
|---|---|---|---|
| 1 | medium | Spec Test plan §Integration strategy mandates real ephemeral `better-sqlite3` ("No mocked datastore — a passing mock is not a passing production"), but AC-02/03/04/06/07/08 use `InMemoryReminderRepository` (`list-handler.test.ts:6`, `list-router.test.ts:3`, `cancel-pending-reminder.test.ts:3`). Only AC-01 read-path hits real SQLite (`sqlite-reminder-repository.test.ts:137`). The `pending→deleted` persist (AC-03) and the source JOIN-by-id read (AC-06) are never exercised against the real schema/index. | **Fix now** → `/sdd:implement`: add real-SQLite integration cases for cancel-persist (AC-03/04) and source JOIN (AC-06). |
| 3 | low | `CancelPendingReminder` throws plain `Error("…not found")` when `findById` returns null (`cancel-pending-reminder.ts:20`); `handleListCancel` only catches `InvalidStateTransitionError` and re-throws the rest (`list-handler.ts:103`) → not-found cancel tap becomes an unhandled error → `bot.catch` (KPI: 0 unhandled list-action errors). ADR-0002 immutable-snapshot contract means stale taps must never crash. | **Fix now** → treat not-found as the same uniform "no longer active" no-op (AC-04). |
| 2 | low | Spec §6 "Latency p95" + SAD QG-3/§8 Observability name a "structured timing log around the list use-case" as the measurement, but no timing instrumentation exists on the `/list` path (`list-handler.ts:68`, `list-active-reminders.ts:65`) → NFR has no verification leaf. | **Fix now** → wrap `listUC.execute()` with the existing structured timing log. |
| 4 | low | Immutable-snapshot guarantee (ADR-0002, AC-03 "frozen point-in-time") holds only because no `editMessageText` is called, but no test asserts it (`list-handler.test.ts:130`); a future refactor adding an edit would pass all current tests. | **Fix now** → assert no `editMessageText` is invoked on the cancel path. |
| 5 | info | AC-08 final-message char budget not re-checked against rendered text — `truncateToFit` budgets `PER_ROW_OVERHEAD_CHARS=40` while `renderListMessage` adds an un-budgeted header + overflow line (`list-handler.ts:39`). Real chrome ≈24 chars, so the 40-char estimate is conservative and 4096 is not at risk. | **Dismissed** — conservative estimate, no real risk; noted only as two-sources-of-truth caveat. |

## Definition of Done (initial pass)

- [x] Independent reviewer ran over the whole feature diff (not per-task).
- [x] Whole §4 US set + §5 AC set traced end-to-end; nothing dropped from the chain.
- [x] Every finding resolved (4 fix-now, 1 dismissed-with-reason); no open Stage-1 finding.
- [x] Review record exists with a CHANGES REQUESTED verdict.

---

## Re-review — 2026-06-15 (after fix commits 3c34f95, 24c9687, d9838c0, 11f5dc6, 652cb50)

**Reviewer:** independent clean-context (`sdd:reviewer`), not the author.
**Verdict: PASS — REVIEW_CLEAN**

**Build / test status (verified independently):**
- `npx tsc --noEmit` — clean, exit 0.
- `npx vitest run` — **158 passed (158), 30 files**, 0 failures.

### Verification of the four prior fix-now findings

| # | Prior finding | Fix verified |
|---|---|---|
| 1 (medium) | Integration tests used `InMemoryReminderRepository` for AC-03/04/06; cancel-persist and source JOIN never hit real SQLite. | FIXED. `src/ports/__tests__/list-integration.test.ts:58-137` spins a real `:memory:` SQLite with `runMigrationsUp`. AC-03 cancel-persist asserts the real row flips to `deleted` (`:68-78`); AC-04 asserts every non-pending state left unchanged in real row (`:80-95`); AC-06 drives `handleSource` through the real `reminders⨝source_snapshots` JOIN for both branches (`:111-137`). Read-path index additionally asserted via EXPLAIN QUERY PLAN at `sqlite-reminder-repository.test.ts:197-206`. |
| 2 (low) | No structured timing log on `/list` path — NFR latency p95 had no verification leaf. | FIXED. `src/ports/handlers/list-handler.ts:77-85` wraps `listUC.execute()` with `module:"list", event:"list_rendered", elapsedMs` log; asserted at `src/ports/__tests__/list-handler.test.ts:103-117`. |
| 3 (low) | Not-found cancel threw plain `Error` → unhandled → `bot.catch` (KPI: 0 unhandled list-action errors). | FIXED. `cancel-pending-reminder.ts:23` throws typed `ReminderNotFoundError`; `list-handler.ts:117-124` catches both `InvalidStateTransitionError` and `ReminderNotFoundError` → uniform no-op. Asserted at `list-handler.test.ts:182-193`. |
| 4 (low) | No test asserted the list message is never edited (ADR-0002 immutable snapshot). | FIXED. `makeCallbackCtx` exposes `editMessageText` (`list-handler.test.ts:126`); cancel happy-path asserts it is never called (`:162`). |

No regressions introduced by the fix commits.

### Stage 1 — US/AC end-to-end trace (re-confirmed)

All five user stories and all eight ACs trace spec → §6 flow → code → asserting test. Nothing dropped.

| ID | Status |
|---|---|
| US-01 / AC-01 | PASS — `ORDER BY scheduled_at ASC, r.id ASC` (`sqlite-reminder-repository.ts:107`), preview cap, tz render, real-SQLite ordering + tie-break asserted. |
| US-04 / AC-02 | PASS — empty path → `EMPTY_MESSAGE` (`list-handler.ts:87`). |
| US-02 / AC-03 | PASS — real-SQLite persist confirms `deleted` (`list-integration.test.ts:68`); no-edit asserted (`list-handler.test.ts:162`). |
| US-02 / AC-04 | PASS — real-SQLite no-change asserted per non-pending state (`list-integration.test.ts:80`); uniform message identical for all end states. |
| US-01,US-02 / AC-05 | PASS — command gate `isOwner` + router-level callback gate `router.ts:59`; full owner tests. |
| US-03 / AC-06 | PASS — real-SQLite both `hasPublicDeepLink` branches asserted (`list-integration.test.ts:111,125`). |
| US-05 / AC-07 | PASS — both entry points produce identical response (`list-router.test.ts:65`). |
| US-01 / AC-08 | PASS — send-count=1 + overflow line asserted (`list-handler.test.ts:83`). |

### Stage 2 — quality (no new findings)

Layering, conventions, security, error/edge handling, and test adequacy all clean. The carry-over info note (two sources of truth for the 4096-char budget — `PER_ROW_OVERHEAD_CHARS=40` vs rendered chrome ≈24 chars) remains a dismissed non-risk as in the first pass.

### Definition of Done (re-review)

- [x] Independent reviewer ran over the whole feature diff with clean context.
- [x] All four prior fix-now findings verified genuinely implemented and pinned by asserting tests.
- [x] No regressions introduced; whole §4 US + §5 AC set re-traced end-to-end.
- [x] No open Stage-1 or Stage-2 findings.
- [x] Review record updated with PASS verdict.
